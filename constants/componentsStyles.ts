import { Colors } from './Colors';

// Styles pour les TextInput
export const textInputStyles = {
    outlineColor: Colors.customeStyle.darkBlue,
    activeOutlineColor: Colors.customeStyle.darkBlue,
    placeholderTextColor: Colors.customeStyle.placeholder,
    textColor: Colors.customeStyle.text,
};

export const buttonStyle = {
    buttonColor: Colors.customeStyle.darkBlue,
    textColor: Colors.customeStyle.text,
}